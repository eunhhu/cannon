# Cannon 구현 상태 — checked native policy projects + typed sessions

## 새로 추가한 Rust 프로젝트 계층

`project::compile_project`는 명시적인 모듈·원본 입력 소유자·정책 공개 범위·타입이 있는 연결을 검사합니다. 다른 모듈의 원본 입력 직접 접근, 비공개 정책 접근, 선언하지 않은 import, 잘못된 타입/연결, 모듈·정책 순환을 실행 전에 거절합니다. 모듈 경계는 이 순수 정책 그래프에 적용되며 임의 Rust 코드나 데이터베이스 권한까지 검사하는 기능은 아닙니다.

`execute_project`와 `execute_project_observed`는 기존 타입 입력 실행기를 그대로 사용해 여러 정책을 의존성 순서로 한 번씩 실행합니다. 각 정책의 실제 입력·중간값·소스 위치와 정확한 프로젝트 객체를 보존합니다. 전체 정책은 eager dataflow로 실행하고, 표현식 내부의 조건 분기·단락 평가는 유지합니다. 정책별 제한 외에 프로젝트 누적 단계·이벤트·문자열 예산과 협력적 취소를 적용합니다. 실패 시 부분 실행을 완료 결과로 반환하지 않습니다.

`project_review::compare_projects`는 소유권·공개 범위·import·연결·타입·소스 텍스트·계산 구조 변화를 구분하고 두 그래프에서 영향 후보를 추적합니다. `review_projects`는 여러 출력의 독립 기대값과 입력을 보존해 이전 프로젝트·후보 자체 사례·후보에 대한 원래 사례 재검사를 수행합니다. 테스트 삭제나 후보 입력/정답 변경이 과거 기대값을 지우지 않습니다. 비어 있는 검증과 중단된 검토를 성공으로 표시하지 않습니다.

실행 예제: `cargo run --locked --offline -p cannon-core --example project`. 세 표현식은 `examples/projects/inventory/`에 있고 모듈 연결은 Rust 예제에서 명시합니다. 런타임 프로젝트 파일 로더나 전체 `.intent` 선언 파서를 추가한 것은 아닙니다. 계약과 제한은 `docs/native-projects.md`에 있습니다.

## 유지되는 Rust 세션·정책 기능

`session::ExpressionSession`은 소스·입력 스키마·ID별 값·기대값을 변경 불가능한 스냅샷으로 묶습니다. 편집 시 이전 실행을 취소하고, 과거 결과를 현재 결과로 승격하지 않습니다. 사람/AI 제안은 동일한 미리보기와 정확한 검토 객체로 메모리 상태에 적용됩니다. 이전 기대값 위반은 명시적으로 인정해야 하며 실패한 후보나 오래된 검토는 적용할 수 없습니다. 이 세션은 여전히 한 스칼라 정책과 한 사례를 소유합니다.

`compile_with_inputs`, `evaluate_bound`, `review_policies`, `cannon-policy`, `cannon-native`를 유지합니다. 새 프로젝트 계층은 별도 파서/계산기를 만들지 않습니다. 안전한 정수·UTF-16·실제 관측·협력적 취소·실행 예산과 기존 기대값은 바꾸지 않았습니다.

## 검증 경로

기존 Rust 테스트 132개에 프로젝트/경계 테스트 62개를 추가했습니다. 그 안에 세 정책을 연결한 재고 입력 3060가지 조합이 포함됩니다. Linux/Windows/macOS의 debug/release 테스트, 엄격한 Clippy, 기존 TypeScript 검사, 독립 표현식 60개와 타입 입력 30개의 양쪽 엔진 대조, 새 프로젝트 예제를 포함한 데모를 CI에 유지/추가했습니다. 기존 Node 22/24 CI도 유지합니다.

성공 여부는 정확한 커밋의 실제 Actions 결과로 확인합니다. 첫 후보에서는 새 프로젝트 테스트 53개가 Linux에서 통과했으나 Clippy 지적이 있었고, 린트 설정/기대값을 약화하지 않고 오류 표현과 import를 수정했습니다. 개발 컨테이너에는 Rust 도구가 없어서 로컬 컴파일은 주장하지 않습니다. 과거 `VALIDATION.json`은 새 변경의 검증 결과가 아닙니다.

## 기존 TypeScript 구현

단일 `.intent` 파일의 레코드·불변조건·규칙·순수 상태 전이·시나리오, 구조/기대값 변경 구분, 이전 사례 재검사, 버전별 근거, 제안·검토·오래된 제안 거부와 CLI를 유지합니다. npm `cannon`의 기본 엔진은 바꾸지 않았습니다.

## 남은 범위

Rust 전체 `.intent` 선언 파서·레코드·상태 전이, 런타임 프로젝트 파일 형식, 증분 다중 파일 분석, 프로젝트 단위 세션 적용, 효과/권한 체계, 미완성 빈칸, LSP/GUI/실제 AI 연결, 운영 저장소·이행·배포 관측, 정형 증명·코드 생성은 완료하지 않았습니다. 이번 프로젝트 모듈 경계는 정책 연결 수준의 검사이며 전체 언어의 소유권/효과 시스템을 완성한 것은 아닙니다.

원격에 있는 기능과 별도의 미검증 전체 파일 workbench를 혼동하지 않습니다. 이전에 차단된 workbench/editor 내용은 이번 변경에서 재전송하지 않았습니다. 이력은 `spec/delivery.md`에 남아 있습니다. 새 기능의 CI 통과도 전체 제품 완성이나 최종 사용자 QA 준비 완료를 의미하지 않습니다.
