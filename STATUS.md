# Cannon 구현 상태 — typed native sessions + TypeScript reference

## 새로 연결한 Rust 세션·검토·적용

`session::ExpressionSession`은 소스, 입력 스키마, 고정 ID별 입력값, 독립 기대값을 변경 불가능한 스냅샷으로 묶습니다. 소스·입력·기대값을 별도로 수정하며, 편집하면 이전 버전의 대기/실행 작업에 취소를 전달합니다. 과거에 완료된 결과는 과거 결과로 남기고 현재 결과로 재사용하지 않습니다.

사람과 AI가 같은 `propose`/`propose_draft` 경로를 사용합니다. 미리보기는 기준 실행, 후보 자신의 입력·기대값 실행, 원래 입력·기대값을 후보 코드에서 재실행한 결과를 따로 보존합니다. 후보가 입력값이나 정답을 바꿔도 과거 사례를 지울 수 없습니다. 이름 변경은 고정 ID로 연결하고, ID 변경·필수 입력 추가·제약 강화는 실제 재검사 오류로 드러납니다.

`apply`는 정확한 검토 객체와 현재 기준 버전을 확인하고 메모리 상태에만 적용합니다. 이전 기대값 위반에는 명시적인 `AcknowledgeChange`가 필요하며, 잘못된 후보·실패한 실행·후보 자신의 기대값 위반·오래된 검토에는 적용되지 않습니다. 적용 후 새 스냅샷에서 다시 실행해야 현재 결과가 됩니다. 인증된 승인, 파일 쓰기, 배포 또는 영속 감사 기록은 아닙니다.

실행 예제: `cargo run --locked --offline -p cannon-core --example review`. 세부 계약과 제한은 `docs/native-sessions.md`에 있습니다. 이전 미검증 세션 후보를 현재 타입 입력 API에 통합한 것이며, 별도 차단된 전체 파일 workbench를 가져온 변경은 아닙니다.

## 유지되는 Rust 정책 기능

`compile_with_inputs`, `evaluate_bound`, `evaluate_bound_observed`로 타입과 고정 ID가 있는 스칼라 입력을 검증하고 실행합니다. `review_policies`는 다중 사례의 입력·기대값 변경과 과거 사례 재검사를 제공합니다. `cannon-policy`, `cannon-native` 명령은 유지합니다. 새 세션도 이 동일한 실행기를 사용하며 파서나 정책 평가기를 복제하지 않습니다.

정수 범위, UTF-16 문자열/소스 위치, 실제 중간값·분기, 협력적 취소와 스트리밍·실행 예산은 유지합니다. 새 세션 미리보기는 실행별 문자열 추적 예산도 명시합니다.

## 검증 경로

기존 Rust 테스트 75개에 세션 테스트 41개와 타입 입력 통합 테스트 16개를 추가했습니다. 기존 기대값이나 검사를 제거하지 않았습니다. CI는 Linux/Windows/macOS의 debug/release 테스트, 엄격한 Clippy, 네이티브 빌드, TypeScript 검사, 독립 표현식 60개와 입력 사례 30개의 양쪽 엔진 대조, 세션 검토를 포함한 데모를 실행합니다. 기존 Node 22/24 CI도 유지합니다.

검증 성공 여부는 정확한 커밋의 Actions 결과로 확인합니다. 개발 컨테이너에는 Rust 도구가 없고 다운로드도 실패하여 로컬 컴파일을 주장하지 않습니다. 과거 `VALIDATION.json`을 이번 변경의 검증 결과로 재사용하지 않습니다.

## 기존 TypeScript 구현

단일 `.intent` 파일의 레코드·불변조건·규칙·순수 상태 전이·시나리오, 구조·기대값 변경 구분, 이전 사례 재검사, 버전별 근거, 제안 미리보기·검토 ID·오래된 제안 거부, CLI를 유지합니다. TypeScript 실행기와 기존 기대값, npm `cannon`의 기본 엔진은 바꾸지 않았습니다.

## 남은 범위

Rust 전체 `.intent` 선언 파서/레코드/상태 전이, 다중 파일·모듈 소유권, 효과 체계, 미완성 빈칸, LSP/GUI/실제 AI 연결, 운영 저장소·이행·배포 관측, 정형 증명, 코드 생성은 완료하지 않았습니다. 세션은 한 정책과 한 사례를 소유하며 전체 프로젝트 관리나 모든 입력의 동등성 증명이 아닙니다. 세션 핸들은 프로세스 내부에서만 유효하며 영속 ID, 서명, 빌드별 검증 근거를 대체하지 않습니다.

별도 전체 파일 Rust workbench와 이전 편집기 전송 제한은 그대로이며, 이 변경에서 차단된 후보를 재전송하지 않았습니다. 기존 전송 경계는 `spec/delivery.md`에 있습니다. 이번 추가가 전체 언어의 완성 또는 최종 사용자 QA 준비 완료를 뜻하지는 않습니다.
