# Cannon 구현 상태 — native typed policies + TypeScript reference

## 새 Rust 실행·검토 경로

`compile_with_inputs`로 타입과 고정 ID가 있는 입력을 선언하고, `evaluate_bound`로 같은 정책을 여러 데이터에 실행합니다. 숫자·불리언·문자열과 양수/음이 아닌 정수 제약을 지원합니다. 누락/추가 입력, 타입 오류, 범위·제약 위반을 계산 전에 거절합니다. 이름이 바뀌어도 ID로 데이터를 연결하며 소스 문자열에 값을 치환하지 않습니다.

`review_policies`는 입력 스키마 변경, 식 구조 변경, 사례의 입력·기대값 변경을 구분합니다. 후보가 테스트를 바꾸거나 삭제해도 이전 입력과 기대값으로 다시 실행합니다. 사례가 없거나 기준 사례가 실패하면 검토 통과로 표시하지 않습니다. 결과에는 두 소스와 스키마, 입력과 기대값, 실제 추적이 남습니다.

Node 없이 실행하는 `cannon-policy eval`과 `cannon-policy demo --json`을 추가했습니다. `docs/native-bound-policies.md`에 명령, API, JSON 형식, 검증·자원·출처 경계를 명시했습니다. 기존 `cannon-native`와 npm `cannon` 명령은 바꾸지 않았습니다.

## 유지되는 Rust 기능

기존 표현식 평가와 `evaluate_observed`는 같은 실행기를 사용합니다. 중간값·분기·UTF-16 소스 위치, 정수 범위, 단락 평가, 협력적 취소, 스트리밍·추적 예산을 유지합니다. `evaluate_bound_observed`는 입력이 있는 실행에 같은 관찰·취소 경로를 제공합니다. 별도 AI 설명기가 실행 사실을 만들지 않습니다.

## 검증 경로

기존 Rust 테스트 39개에 입력·검토·CLI 테스트 36개를 추가했습니다. 재고 입력 3,060가지 조합도 검사합니다. 기존 독립 표현식 사례 60개를 유지하며 입력이 있는 독립 사례 30개와 양쪽 엔진의 실제 실행 본문·소스 위치 대조를 추가했습니다. Linux/Windows/macOS에서 debug/release, 엄격한 Clippy, 잠금 파일 기반 TypeScript 검사, 두 대조 실행기와 데모를 수행합니다. 기존 Node 22/24 CI도 유지합니다.

성공 여부는 정확한 커밋의 실제 Actions 결과로 확인합니다. 이번 로컬 환경에서는 새 입력 사례 30개의 TypeScript 기준 실행을 확인했으나 Rust 컴파일러는 없었습니다. 과거 `VALIDATION.json`은 새 Rust 검증 결과로 재사용하지 않습니다.

## 기존 TypeScript 구현

단일 `.intent` 파일의 레코드·불변조건·규칙·순수 상태 전이·시나리오, 구조와 기대값의 변경 구분, 이전 사례 재검사, 버전별 근거, 제안 미리보기·검토 ID·오래된 제안 거부, CLI를 유지합니다. TypeScript 실행기와 기존 기대값은 이번 변경에서 수정하지 않았습니다.

## 남은 범위

Rust 전체 `.intent` 선언 파서/레코드/상태 전이, 다중 파일·모듈 소유권, 효과 체계, 미완성 빈칸, LSP/GUI/실제 AI 연결, 운영 저장소·이행·배포 관측, 정형 증명, 코드 생성은 완성되지 않았습니다. 새 Rust 정책 보고서는 전체 엔진의 검증 근거·제안 승인·파일 적용 프로토콜을 대체하지 않으며, 서명·빌드 출처 인증·검증 결과의 최신성 판정이 없습니다.

별도 전체 파일 Rust workbench와 이전 편집기 전송 제한은 그대로이며, 이번 변경에서 차단된 후보를 재전송하지 않았습니다. 기존 전송 경계는 `spec/delivery.md`, Rust 표현식·관찰 계약은 `docs/native-expression-slice.md`와 `docs/native-observation.md`를 함께 봅니다. 현재 기능이 전체 언어 또는 최종 QA 준비 완료라는 뜻은 아닙니다.
