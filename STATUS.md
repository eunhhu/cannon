# Cannon 구현 상태 — 0.1.0

## 이 저장소에 구현됨

단일 `.intent` 파일의 파서, 안정적인 ID를 가진 의미 모델, 기본 타입·참조 검사, 규칙과 순수 상태 전이의 실행기가 있습니다. 정수 범위·정제 타입·레코드 불변조건을 실행 경계에서 검사합니다.

실제 중간값·분기·거절 이유를 소스 버전에 연결합니다. 구조 변경, 입력 변경, 기대값 변경과 의존성 기반 영향 후보를 구분합니다. 후보 테스트와 별개로 이전 입력·기대값을 재검사하며 테스트 삭제가 과거 검증 사례를 지우지 않습니다.

사람·AI 공통 제안 API, 결정적인 미리보기, 정확한 검토 ID, 오래된 제안 거부, 협력적 파일 잠금·원자적 교체, 데모용 메모리 비교 쓰기, CLI와 90개 자동 테스트를 포함합니다.

## 이번 전달에서 빠진 것

VS Code 확장 소스 업로드는 GitHub 도구가 보안 상태를 판정하지 못해 차단했습니다. 차단된 코드를 다른 경로나 인코딩으로 전송하지 않았습니다. 이 원격 전달본은 편집기 진입점, 관련 구성과 5개 편집기 테스트를 제외한 독립적인 CLI 패키지입니다. 전체 로컬 패키지는 별도로 보존했습니다. 상세 내역은 `spec/delivery.md`를 확인하세요.

## 검사 범위

Node.js 22.16.0 / Linux에서 CLI 전달본의 TypeScript 빌드·90개 테스트·데모를 실행했고 모두 통과했습니다. 재고 입력 3,060가지 조합도 포함됩니다. 로컬 레지스트리 DNS가 차단되어 새 `npm ci`를 완료하지 못했고, 미리 설치된 TypeScript 5.8.3, @types/node 22.19.7, undici-types 6.21.0을 사용했습니다. 잠금 파일은 @types/node 22.15.33을 지정하므로 고정 의존성 설치 검증은 별도입니다.

로컬 캡처 결과는 `VALIDATION.json`에 기록했습니다. 원격 CI는 Actions 탭의 해당 커밋 결과를 확인해야 하며 실행 전에 성공으로 표시하지 않습니다. 실제 VS Code Extension Host, AI 제공자, 운영 배포는 검사하지 않았습니다.

## 아직 구현하지 않음

다중 파일/모듈 소유권, 열거형·패턴 매칭·제네릭, 미완성 빈칸 실행, Langium/LSP, 영속 데이터베이스, 스키마 이행, 외부 효과 검사, 운영 추적, 실제 AI 제공자/MCP 연결, 정형 증명, 코드 생성 백엔드는 없습니다.

`transition`은 저장이 아니라 새 상태를 계산하는 순수 함수입니다. 메모리 저장소의 비교 쓰기는 단일 프로세스의 동기 동작만 다룹니다. 분산 트랜잭션이나 재시도 정책을 구현한 것으로 해석하면 안 됩니다.
