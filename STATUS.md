# Cannon 구현 상태 — reference + observable native expressions

## 현재 Rust 구현

`crates/cannon-core`는 외부 Rust 패키지와 Node 호출 없이 독립적인 표현식을 파싱·타입 검사·실행합니다. 안전한 정수 범위, UTF-16 문자열과 소스 위치, 조건 분기·단락 평가, 실제 실행 기록을 다룹니다. `cannon-native` CLI가 이 경로를 사용합니다.

`evaluate_observed`는 같은 실행기에 실시간 관찰 콜백, 공유 가능한 단방향 취소 신호, 추적 보관 여부, 관측된 문자열 값의 예산을 추가합니다. 취소는 `CANCELLED` 오류이며 완료 결과가 아닙니다. 스트리밍해도 추적 이벤트 수 제한을 우회하지 않습니다. 기존 `evaluate`의 기본 결과·오류·기록·단계 수 계약은 유지합니다. 자세한 경계와 사용법은 `docs/native-observation.md`를 확인하세요.

## 검증 경로

기존 Rust 테스트 21개에 관찰·취소 테스트 18개를 추가했습니다. CI는 Linux/Windows/macOS에서 debug와 release 테스트, 엄격한 Clippy, 기존 TypeScript와의 60개 독립 사례 및 실행 기록 대조, 두 네이티브 데모를 수행합니다. 기존 Node 22/24 CI도 유지합니다. 성공 여부는 해당 커밋의 실제 Actions 결과로 확인해야 합니다. `VALIDATION.json`은 과거 TypeScript 전달 기록이며 새 Rust 검증 결과로 재사용하지 않습니다.

## 아직 원격에 없는 전체 파일 Rust 후보

이 대화의 별도 Rust workbench 소스 후보는 전체 `.intent` 파일, 레코드/규칙/전이, 비교·이전 기대값 재검사·제안 API·호스트 연결 코드를 담고 있지만, 전체 파일 파서/검사기 기록 요청이 보안 판정에서 차단되었습니다. 후보는 컴파일·테스트가 확인된 원격 배포본이 아닙니다. 이 커밋은 그 후보를 다른 경로로 전달하지 않으며, `crates/cannon-workbench`를 등록하거나 기본 엔진을 전환하지 않습니다.

## 기존 TypeScript 구현

단일 `.intent` 파일의 파서, 안정적인 ID가 있는 의미 모델, 타입·참조 검사, 레코드·불변조건, 규칙·순수 상태 전이, 시나리오 실행을 지원합니다. 구조·입력·기대값 변경을 분리하고, 후보가 테스트를 변경하거나 삭제해도 이전 입력과 기대값을 재검사합니다. 실제 실행 기록, 버전별 근거, 제안 미리보기·정확한 검토 ID·오래된 제안 거부, CLI가 있습니다. npm의 `cannon` 명령은 아직 이 엔진입니다.

## 남은 구현 및 검증 경계

Rust 전체 파일 엔진의 원격 전달, 다중 파일/모듈 소유권, 효과 체계, 열거형·패턴 매칭·제네릭, 미완성 빈칸 실행, LSP/GUI/실제 AI 연결, 운영 데이터베이스·스키마 이행·배포 관측, 정형 증명, 코드 생성 백엔드는 완료하지 않았습니다. 표현식 실행 취소는 전체 프로젝트 증분 분석이나 강제 시간 제한이 아닙니다.

이전 편집기 파일 전송 경계는 `spec/delivery.md`에 남아 있습니다. 기존 `transition`은 상태를 계산하는 순수 함수이며, 메모리 저장소는 단일 프로세스의 예시입니다. 운영용 데이터베이스나 분산 트랜잭션으로 해석하면 안 됩니다.
