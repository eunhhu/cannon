# Cannon 구현 상태 — TypeScript reference + native expression slice

## 새로 추가된 Rust 실행 경로

`crates/cannon-core`에 외부 패키지 없는 Rust 라이브러리와 `cannon-native` CLI를 추가했습니다. 독립적인 순수 표현식의 파싱·타입 검사·안전한 정수 계산·조건 분기·단락 평가·실행 추적을 구현합니다. UTF-16 문자열/소스 위치와 실행 예산을 유지합니다.

전체 `.intent` 파일, 레코드/규칙/전이 선언, 변경 비교, 이전 기대값 재검사, 제안/근거 프로토콜은 아직 Rust로 이식하지 않았습니다. 기존 TypeScript `cannon` 명령은 그대로이며 기본 엔진을 바꾸지 않았습니다. 새로운 표현식 보고서는 기존 실행 근거나 승인 증명서가 아닙니다.

독립 기대값 60개를 TypeScript와 Rust에서 확인하고, 실행된 사례는 중간값·분기·단계 수·소스 위치까지 대조하는 검사를 추가했습니다. Linux/Windows/macOS 네이티브 CI와 기존 Node 22/24 CI를 따로 유지합니다. 자세한 실행법과 남은 범위는 `docs/native-expression-slice.md`를 확인하세요.

이번 로컬 환경에서 기존 코어 테스트 76개와 기준 사례 60개를 TypeScript 5.8.3 / Node 타입 25.1.0으로 검사했습니다. 로컬 Rust 컴파일러와 새 잠금 파일 설치는 없었습니다. 네이티브/원격 검증 성공 여부는 해당 커밋의 Actions 결과로 확인해야 합니다. 기존 `VALIDATION.json`은 과거 TypeScript 전달 기록이지 이 변경의 검증 결과가 아닙니다.

## 기존 TypeScript 구현

단일 `.intent` 파일의 파서, 안정적인 ID를 가진 의미 모델, 기본 타입·참조 검사, 규칙과 순수 상태 전이의 실행기가 있습니다. 정수 범위·정제 타입·레코드 불변조건을 실행 경계에서 검사합니다.

실제 중간값·분기·거절 이유를 소스 버전에 연결합니다. 구조 변경, 입력 변경, 기대값 변경과 의존성 기반 영향 후보를 구분합니다. 후보 테스트와 별개로 이전 입력·기대값을 재검사하며 테스트 삭제가 과거 검증 사례를 지우지 않습니다.

사람·AI 공통 제안 API, 결정적인 미리보기, 정확한 검토 ID, 오래된 제안 거부, 협력적 파일 잠금·원자적 교체, 데모용 메모리 비교 쓰기, CLI와 90개 자동 테스트를 포함합니다. 이번 변경에서 이 구현과 기존 테스트 기대값은 수정하지 않았습니다.

## 이전 전달 경계

VS Code 확장 소스 업로드는 GitHub 도구가 보안 상태를 판정하지 못해 차단했습니다. 차단된 코드를 다른 경로나 인코딩으로 전송하지 않았습니다. 원격 전달본은 편집기 진입점, 관련 구성과 편집기 테스트를 제외한 독립적인 CLI 패키지입니다. 이 Rust 변경도 해당 파일을 다시 전송하거나 차단을 우회하지 않습니다. 상세 내역은 `spec/delivery.md`를 확인하세요.

## 아직 구현하지 않음

다중 파일/모듈 소유권, 열거형·패턴 매칭·제네릭, 미완성 빈칸 실행, Langium/LSP, 영속 데이터베이스, 스키마 이행, 외부 효과 검사, 운영 추적, 실제 AI 제공자/MCP 연결, 정형 증명, 코드 생성 백엔드는 없습니다.

`transition`은 저장이 아니라 새 상태를 계산하는 순수 함수입니다. 메모리 저장소의 비교 쓰기는 단일 프로세스의 동기 동작만 다룹니다. 분산 트랜잭션이나 재시도 정책을 구현한 것으로 해석하면 안 됩니다. 실제 VS Code Extension Host, AI 제공자, 운영 배포 검증도 없습니다.
