# Cannon 구현 상태 — typed native policy sessions + TypeScript reference

## 이번에 연결한 Rust 편집·검토 경로

`session::PolicySession`에 소스, 안정적인 ID가 있는 입력 스키마, 독립 사례와 기대값을 함께 보관하는 변경 불가능한 스냅샷을 추가했습니다. 기존 `compile_with_inputs`, `evaluate_bound_observed`, 정책 비교기를 재사용하며 별도의 실행기를 만들지 않습니다.

직접 소스 편집은 기존 스키마·기대값을 유지하고, 사례 변경은 별도 동작입니다. 편집/종료는 이전 실행과 검토에 협력적 취소를 전달합니다. 완료된 결과는 정확한 과거 버전에 남고, 같은 버전의 여러 요청은 `RunTicket`으로 구분합니다.

제안은 현재 상태를 수정하지 않습니다. 검토는 코드·스키마·사례 입력·기대값 변화를 구분하며, 삭제/변경된 후보 테스트와 독립적으로 이전 사례를 재실행합니다. 적용은 정확한 현재 기반과 검토한 후보만 사용합니다. 비어 있거나 실패한 검증, 실행 실패는 승인으로 덮지 않습니다. 과거 반환값이 달라지는 정책 변경은 `AcknowledgeChangedValues`로 명시해야 합니다. 적용 후에는 새 스냅샷에서 다시 실행해야 현재 결과를 얻습니다.

이 API는 메모리 내 상태만 바꿉니다. `cargo run --locked --offline -p cannon-core --example session_review`로 실행→제안→과거 요구사항 재검사→기본 적용 거절→명시적 변경 인정→새 버전 실행→오래된 검토 거절을 확인합니다. 상세 계약은 `docs/native-sessions.md`에 있습니다.

## 유지한 실행 경로

Rust 표현식·타입 입력·ID 기반 데이터 연결, 정수/정제 타입 검사, UTF-16 값과 위치, 실제 실행 추적·단락 평가·실행 예산·스트리밍·취소를 유지합니다. 기존 `cannon-native`, `cannon-policy` 명령과 보고서 계약은 그대로입니다.

TypeScript 참조 엔진의 전체 단일 `.intent` 레코드·규칙·전이·시나리오, 변경 비교, 버전별 근거, 제안/파일 적용, npm `cannon` 명령은 유지합니다. 기존 계산 코드나 테스트 기대값을 이번 변경에 맞춰 수정하지 않습니다.

## 검증 경로

세션 통합 테스트 44개와 버전 고갈 단위 테스트 1개를 추가했습니다. CI에서 기존 검사와 함께 Linux/Windows/macOS의 Rust debug/release, 엄격한 Clippy, 60개 표현식과 30개 입력 사례의 TypeScript/Rust 대조, 새 세션 예제와 기존 데모를 실행합니다. Node 22/24 CI도 유지합니다. 성공 여부는 정확한 커밋의 실제 Actions 결과로 확인합니다. 로컬에는 Rust 컴파일러가 없으며 과거 `VALIDATION.json`을 새 검증 결과로 재사용하지 않습니다.

## 남은 범위

이 Rust 경로는 단일 타입 정책 문서입니다. 전체 `.intent` 선언 파서/레코드/상태 전이, 다중 파일·모듈 소유권, 효과 체계, 미완성 빈칸 실행, LSP/GUI/실제 AI 제공자, 운영 저장소·데이터 이행·배포 관측, 정형 증명과 코드 생성은 완료되지 않았습니다.

세션 핸들은 프로세스 내부 식별자이지 직렬화된 검증 근거나 서명된 승인서가 아닙니다. 파일 저장·배포·원격 인증을 구현한 것으로 해석하지 않습니다. 이전 전체 파일 Rust workbench/편집기 전송 경계는 유지하며 차단된 코드를 이 변경에 포함하지 않습니다. `spec/delivery.md`, `docs/native-bound-policies.md`, `docs/native-observation.md`를 함께 봅니다. 현재 기능은 최종 전체 QA 준비 완료를 뜻하지 않습니다.
