# Cannon: production 플랫폼 로드맵

작성: 2026-09-09 · 계획 버전: 1 · 조사 기준: `7b856994e92149f223e37a08a714c0fec6f728ca`

**이 문서는 앞으로 구현할 전략이다. 계획의 존재는 기능 구현, QA 또는 출시 승인을 뜻하지 않는다.** 현재 사실은 [STATUS.md](STATUS.md), 고정한 출발점은 [baseline.md](docs/strategy/baseline.md)를 따른다. 이번 의뢰는 계획 작성과 저장소 반영이며, 새 운영 계정·유료 자원·데이터 전송·배포 권한의 승인이 아니다.

## 1. 최종 제품의 정의

Cannon은 사람이 아키텍처, 데이터 의미, 업무 규칙을 직접 편집하고, AI의 제안을 같은 실행·검증·변경 검토 경로에서 다루는 **local-first 개발 플랫폼**이다. 대화 기록을 지워도 소스와 저장소만으로 의도, 구현 상태, 검증 범위, 미결정 사항을 복원할 수 있어야 한다.

최종 사용자 경험은 다음을 끝까지 연결한다.

1. 프로젝트를 열어 모듈의 책임, 소유 데이터, 외부 효과와 미해결 결정을 파악한다.
2. 실행 가능한 모델·규칙·상태 전이를 수정하고 실제 데이터의 중간값과 반례를 확인한다.
3. AI 제안을 받되 모델/정책/테스트 기준/권한/데이터 이행의 변경을 분리해서 검토한다.
4. 검토한 정확한 변경을 적용하고, 독립적인 원래 기대값으로 재검사한다.
5. 패키지·실행기·환경이 고정된 산출물을 배포하고, 실제 버전과 관측을 설계에 연결한다.
6. 실패했을 때 코드, 데이터, 외부 효과를 구분하여 복구한다.

모든 분야에 같은 DSL을 강요하지 않는다. 공통 의미 엔진·타입·효과·도구 계약 위에 도메인 표현과 호스트 연동을 확장한다. **모든 프로그램을 지원한다는 주장과, 명시된 지원 범위에서 production 품질이라는 주장은 구별한다.**

## 2. 제품 단계와 비목표

| 단계 | 사용자에게 제공할 것 | 이 단계만으로 주장하지 않는 것 |
|---|---|---|
| 현재 | Rust 정책/프로젝트 그래프와 세션 API, TypeScript 전체 단일 파일 참조 엔진 | 완성된 언어·IDE·운영 런타임 |
| Local alpha | Rust가 파일 프로젝트를 읽고, 모델·규칙·검토·복구 가능한 적용을 수행 | 운영 서비스, 호환성 보장 |
| Team beta | IDE와 선택적 AI 연결, 팀 검토, 하나의 실제 저장소 어댑터, 설치 가능한 배포본 | 무제한 워크로드·멀티테넌트 SaaS |
| Production v1 | 명시된 업무 정책/워크플로/데이터 변환 범위에서 지원·업그레이드·복구·보안·운영 검증 완료 | 임베디드/하드 실시간/모든 언어 기능 |
| 확장 플랫폼 | 버전 있는 확장 SDK, 추가 도메인 키트·실행 대상·팀 서비스 | 미검증 타깃의 자동 동등성 |

v1은 재고 예제를 하드코딩한 제품이 아니다. 최소 두 도메인(업무 워크플로와 데이터 변환)의 변경 시나리오를 지원해야 한다. 독립 IDE, 공개 확장 마켓, 자체 호스팅 모델, LLVM/JIT, 브라우저 런타임, 실시간 협업 서버를 첫 출시의 선행조건으로 만들지 않는다. 다만 [후반 단계](#5-확장-플랫폼)에서 따로 구현·검증한다.

## 3. 변하지 않는 원칙

- Rust가 장기 공통 코어다. JS/TS는 얇은 편집기·표현 어댑터와 임시 참조 구현에 둔다. 엔진 구현 언어와 Cannon 프로그램의 실행 타깃은 별도 결정이다.
- 사람이 읽는 실행 소스가 원본이다. 다이어그램·색인·보고서·AI 요약은 파생물이며 또 다른 정책 원본이 아니다.
- 타입 검사, 테스트, 증명, 운영 관측, AI 추정은 서로 다른 근거다. 미확인 상태를 성공으로 바꾸지 않는다.
- 기존 ID·정수 범위·UTF-16 위치·평가 순서·이전 기대값 재검사를 유지한다. 변경하려면 언어 버전과 이행 계획이 필요하다.
- 실제 외부 효과는 순수 미리보기와 분리한다. AI 없이도 핵심 개발·검증이 가능해야 한다.
- 테스트를 새 출력에 맞춰 고쳐서 통과시키지 않는다. 승인된 정책 변경도 과거의 실패 근거를 지우지 않는다.

## 4. 마일스톤과 출구 조건

일정이 아니라 검증 가능한 출구 조건으로 단계가 끝난다. 아래 번호는 전체 직렬 작업 순서가 아니다. 병렬 시작 조건은 [tasks.json](docs/strategy/tasks.json)의 의존 관계가 원본이다. 모든 신규 작업은 아직 `planned`다.

| 단계 | 중심 결과 | 통과해야 할 게이트 | 작업 |
|---|---|---|---|
| M0 기반 고정 | 재현 가능한 환경, 현재 계약·범위·벤치마크·위협 모델 | 새 환경에서 기존 CI 재현; 기존 기능/갭/위험/의사결정 기록; 작업 그래프 검사 | CN-001~006 |
| M1 Rust 언어 코어 | 전체 단일 파일 문법, 레코드·불변조건·규칙·전이·진단 | 독립 적합성 사례와 TS 차등검사; 잘못된 입력/Unicode/한도/취소 검증; 호스트 효과 없음 | CN-007~012 |
| M2 파일 프로젝트와 신뢰할 수 있는 변경 | 매니페스트/잠금, 다중 파일, 영속 식별, 프로젝트 검토·복구 가능한 적용 | 전후 소스·원래 테스트가 보존됨; 충돌/중단/재시작에서 조용한 데이터 손실 없음; staged 결과를 현재 근거로 승격하지 않음 | CN-013~018 |
| M3 사람이 직접 쓰는 local alpha | 언어 확장, LSP, VS Code 검토 UI, 즉시 실행·진단 | 실제 Extension Host E2E; 오프라인 작업; 미완성 상태와 실제 실행을 구분; 두 도메인 변경 흐름 | CN-019~023 |
| M4 선택적 AI 어시스트 | 제한된 문맥 조회, MCP/공급자 어댑터, 구조화된 제안·반례 | 가짜 공급자 결정적 검사 + 승인된 실제 연결; 외부 전송 표시; AI가 기대값/권한을 조용히 변경하지 못함 | CN-024~027 |
| M5 운영 가능한 실행·데이터 경계 | 효과 모델, 로컬/서비스 호스트, 단일 운영 DB 어댑터, 이행 | 미리보기의 실제 쓰기 0; 동시성/중복 요청/장애/데이터 전환·복구 시험; 운영 상태를 코드 존재와 분리 | CN-028~032 |
| M6 배포·성능·보안 강화 | 설치 산출물, 업데이트/롤백, 공급망, 부하·복구 | 지원 OS/CPU별 실제 설치; 독립 보안 검토; 정한 성능 예산; 손상·크래시 복구; 운영 런북 | CN-033~037 |
| M7 production v1 | 파일→IDE→AI 제안→실행→검토→배포→관측의 연결 | 베타 사용 관측, 인간 최종 QA, 알려진 차단 결함 해소, 복구 훈련, 출시 책임자 승인 | CN-038~040 |
| M8 확장 플랫폼 | SDK·도메인 키트·추가 타깃·선택적 팀 서비스 | 외부 확장이 진단/추적/리팩터링 계약을 유지; 타깃별 적합성·격리·지원 기준 통과 | CN-041~045 |

**임계 경로:** 계약 고정 → Rust 의미 동등성 → 파일/버전/검토/적용 → 실제 IDE와 AI 연동 → 운영 어댑터·배포 안전성 → 파일럿/최종 QA. 보안·성능 측정·독립 사례 설계는 M0부터 병렬로 시작하고 마지막에 덧붙이지 않는다.

## 5. 확장 플랫폼

M8에서도 공통 코어를 모든 분야에 맞춰 무조건 비대하게 만들지 않는다. 확장은 타입 규칙, 오류, 소스 매핑, 추적, 리팩터링, 직렬화 이행, 비용 모델을 함께 제공하는 버전 있는 계약이다. 최소 하나의 비재고 도메인 키트를 코어 수정 없이 구현하여 확장성을 증명한다.

추가 타깃은 필요가 입증된 순서로 정한다. 첫 후보는 WASM 또는 호스트 라이브러리 연동이며, 네이티브 코드 생성은 디버그·근거·오류 의미 보존을 검증한 뒤 채택한다. 저수준 메모리/자원 제어와 실시간 프로파일은 별도 언어 설계다. Rust로 구현했다고 Cannon에 자동으로 생기지 않는다.

선택적 팀 서버는 프로젝트/조직 권한, 감사, 키 관리, 데이터 삭제, 동시 편집과 충돌 모델이 필요하다. 로컬 단일 사용자 안전성을 멀티테넌트 격리의 증거로 재사용하지 않는다. 클라우드 없이도 v1 핵심 기능을 유지한다.

## 6. 실행 전략

[에이전트 실행 규칙](docs/strategy/agent-execution.md)에 따라 coordinator 한 명이 계약과 통합을 관리한다. 처음에는 구현 lane 2개와 독립 검증 lane 1개로 시작한다. 같은 핵심 타입/직렬화/문법 파일은 동시에 수정하지 않는다. 더 많은 에이전트는 기능 수가 아니라 재작업률·통합 대기·검토 비용을 측정한 뒤 추가한다.

첫 파동은 CN-001(환경·출발점), CN-005(위협 모델), CN-006(작업표 검사)다. 인터페이스가 고정되면 언어 코어, 독립 적합성 사례, 프로토콜/IDE를 별도 lane으로 진행한다. UI는 계약 fixture로 먼저 만들 수 있지만 실제 엔진 E2E 전에는 완료가 아니다.

전체 QA를 소유자에게 매번 넘기지 않는다. 에이전트가 빌드·자동검증·통합·실패 복구까지 수행한다. 소유자는 모호한 업무 정책, 공개 호환성 파괴, 실제 데이터/권한/비용, 라이선스, 최종 출시를 결정한다. 모호한 결정은 [decisions.md](docs/strategy/decisions.md)에 모아 요청하고, 영향 없는 작업은 계속한다.

## 7. production 완료 판정

[quality-release.md](docs/strategy/quality-release.md)의 G0~G7을 모두 통과해야 v1이다. 문서 작성, 테스트 개수, 데모 성공, CI 초록색 하나만으로 통과하지 않는다. 빈 시나리오·누락된 플랫폼·시험하지 않은 공급자는 `not-tested`다.

최종 QA 패키지는 설치 파일과 검증 방법, 지원 범위, 두 도메인의 전체 시나리오, 정확한 커밋/산출물 해시, 실패·복구 결과, 외부 전송/권한 안내, 알려진 제한, 업그레이드/롤백, 운영 담당자를 포함한다. 인간 QA는 마지막 제품 판정이며 자동검증의 대체물이 아니다.

## 8. 읽기 순서와 다음 작업

새 에이전트는 [AGENTS.md](AGENTS.md) → [strategy 안내](docs/strategy/README.md) → [작업표](docs/strategy/tasks.json) → 배정된 작업에 연결된 계약만 읽는다. 이 대화나 전체 저장소를 무조건 문맥에 넣지 않는다.

다음 구현의 첫 단위는 **CN-001**이다. 최신 `main`을 재조회하고, 이미 구현된 기능을 재작업하지 말고 현재 환경에서 기준 검증을 재현한다. CN-005/CN-006은 쓰기 경계가 겹치지 않는 별도 작업 공간에서 병렬 가능하다.
